version: "3.1"

services:
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: D1ymf8wyQEGthFR1E9xhCq
      POSTGRES_USER: LTIdbUser
      POSTGRES_DB: LTIdb
    ports:
      # Host 5433 -> container 5432. Use 127.0.0.1 in DATABASE_URL (not localhost) on Windows if ::1 conflicts with other tools.
      - "5433:5432"
